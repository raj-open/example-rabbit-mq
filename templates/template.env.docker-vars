# ----------------------------------------------------------------
# .env variables to be overwritten for docker
# ----------------------------------------------------------------

# must coincide with .env > DOCKER_CONTAINER_...
HTTP_PORT=5000
HTTP_PORT_RABBIT_QUEUE=5672
HTTP_PORT_RABBIT_WEB=15672

# --------------------------------
# NOTE: Do not change these values
# --------------------------------

PYTHON_PATH="python3"
HTTP_IP="0.0.0.0"

# since main service and queue are in same network
SHARED_NETWORK=true

# forcibly censor sensitive variables
HTTP_PASSWORD=''
RABBITMQ_DEFAULT_PASS=''
